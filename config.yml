logging:
  level: INFO
  loggers:
    org.nil: DEBUG

# Database settings.
database:
    # the name of the JDBC driver, mysql in this case
    driverClass: com.mysql.jdbc.Driver
    # the username
    user: appuser
    # the password
    password: appuser123
    # the JDBC URL; the database is called DWGettingStarted
    url: jdbc:mysql://localhost:3306/nil_db

swagger:
    resourcePackage: org.nil.movieMania.resources
